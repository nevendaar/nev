<html lang="ru">
    <head>
        <%= charset_and_ie_support_tags %>
        <title><%= @params[:title] || '$MODULE_NAME$ - $SITE_NAME$' %></title>
        <%= meta_tags %>
        $GLOBAL_AJAXJS$
        <%= stylesheet_link_tag %>
        <%= javascript_include_tag %>
    </head>
    <body>
        <main class="bordered<%= ' vk-init' if @params[:vk][:init] %>">
            <% yield if block_given? %>
        </main>
    </body>
</html>
