<html lang="ru">
    <head>
        <%= charset_and_ie_support_tags %>
        <title><%= @params[:title] || "#{c '$MODULE_NAME$'} - #{c '$SITE_NAME$'}" %></title>
        <%= meta_tags %>
        <%= c '$GLOBAL_AJAXJS$' %>
        <%= stylesheet_link_tag %>
        <%= javascript_include_tag %>
    </head>
    <body>
        <div class="wrapper">
            <%= c '$GLOBAL_HEADER$' %>
            <div class="middle">
                <main class="wide_content<%= ' vk-init' if @params[:vk][:init] %>">
                    <% yield if block_given? %>
                </main>
            </div>
            <%= c '$GLOBAL_FOOTER$' %>
        </div>
    </body>
</html>
